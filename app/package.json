{
  "name": "app",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:spacing": "! grep -rnE 'style=\\{\\{[^}]*(padding|margin)' src --include='*.tsx' | grep -v 'global-error' | grep -v 'charts/index.tsx' || (echo '\\nInline padding/margin found. Use Tailwind spacing classes — they work now that the unlayered * reset is gone (see globals.css header).' && exit 1)",
    "verify": "npm run lint && npm run lint:spacing && npx tsc --noEmit && npm test && npm run build",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.2",
    "lucide-react": "^0.562.0",
    "next": "^16.3.0",
    "next-themes": "^0.4.6",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "sonner": "^2.0.7",
    "zustand": "^5.0.9"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.3.0",
    "tailwindcss": "^4",
    "tw-animate-css": "^1.4.0",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
